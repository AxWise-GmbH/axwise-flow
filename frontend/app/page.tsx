'use client';

/**
 * Main application page that serves as the entry point for users
 * Temporarily simplified for debugging
 */
export default function HomePage(): JSX.Element {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold mb-4">Debug Mode</h1>
      <p>Simplified page for debugging Server/Client Component issues</p>
    </div>
  );
}